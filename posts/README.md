# Post jobs

Put WordPress publishing jobs here as UTF-8 JSON files.

Recommended filename:
`<post-id>-<slug>.json`

For existing posts, always include both:
- `id`
- `expect_slug`

This provides a safety check before the update is sent.
